# Grid Warm-Up

Start from the stacked blocks and introduce the grid layout model.

## Requirements

- Turn the container into a grid with three equal columns and a consistent gap between items.
- Keep the items centered with flexbox so the numbers remain legible.
- Use color to highlight alternating cells (already provided) and observe how grid distributes the children without additional positioning rules.
